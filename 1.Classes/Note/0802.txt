1)문서안에 새로운 Node를 생성하고자 할 때
>createElement("p")

2) 특정 태그 형식의 Node안에 입력될 텍스트를 생성하고자 할 때
>createTextNode("입력하고 싶은 text")

3)특정 노드 요소를 상위 Node에 자식요소로 삽입하고자 할 때 // 무조건 상위노드의 가장 마지막 자식요소로만 삽입!
>상위노드명appendChild("자식요소 노드")

4)특정 노드에 속성을 생설하고 적용하고자 할 때
>속성 생성 : createAttribute("속성명")
>생성된 속성 값 적용 : 노드명.value = 속성값

5)